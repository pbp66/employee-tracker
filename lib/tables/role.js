export default class Role {
    constructor(name, salary, department, id) {
        this.title = name;
        this.salary = salary;
        this.department = department;
        this.id = id;
    }
}